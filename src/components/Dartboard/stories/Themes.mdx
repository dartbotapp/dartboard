import { Meta, Canvas, Title, Subtitle, Unstyled } from '@storybook/blocks';
import { Token, baseTokens } from '../theme';
import * as ThemeStories from './Theme.stories';

export const colorSample = (c) =>
  (<div className="color-sample" style={{ backgroundColor: c  }}></div>);
export const token = (t) =>
  (<code>{t}</code>);

<Meta title="Components/Dartboard" name="Themes" />

<Title>Themes</Title>
<Subtitle>Customize the rendering of the dartboard</Subtitle>
<br /><br />

### A default dartboard, in all it's glory
<Canvas of={ThemeStories.ColorPallets} />

### Try different color pallets
<Canvas of={ThemeStories.Colors} />

### Wire width
{token(Token.wireWidth)}
<Canvas of={ThemeStories.WireWidth} />

### Wire shadow blur
{token(Token.wireShadowBlur)}
<Canvas of={ThemeStories.WireShadowBlur} />

### Wire shadow offset
{token(Token.wireShadowOffsetX)}<br />
{token(Token.wireShadowOffsetY)}
<Canvas of={ThemeStories.WireShadowOffset} />

### Wire overhang
{token(Token.wireRingOffset)}
<Canvas of={ThemeStories.WireRingOffset} />

### Show numbers
{token(Token.numberShow)}
{token(Token.numberWireShow)}
<Canvas of={ThemeStories.NumberShow} />

### Number inset
{token(Token.numberInset)}
<Canvas of={ThemeStories.NumberInset} />
